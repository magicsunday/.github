# Pinned tool version for the reusable yamllint workflow — see AGENTS.md.
#
# yamllint.txt is the compiled, hash-locked closure — never hand-edit it.
# Regenerate after bumping the version below: see AGENTS.md's "Each .txt here
# is a hash-locked closure" bullet for the exact command (platform/pip-tools
# pins and working directory matter — don't improvise a shorter one).
yamllint==1.38.0
