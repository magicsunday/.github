# Pinned cmarkgfm version used by readme_catalog_check.py to render
# README.md through GitHub's own cmark-gfm C library instead of
# hand-replicating GFM's block-precedence rules (indented/fenced code,
# HTML comments, ...) in a line-oriented tokenizer - see AGENTS.md for
# why the pin lives here.
#
# cmarkgfm.txt is the compiled, hash-locked closure - never hand-edit it.
# Regenerate after bumping the version below: search AGENTS.md for
# "hash-locked closure" for the exact command (platform/pip-tools pins and
# working directory matter - don't improvise a shorter one).
cmarkgfm==2025.10.22
